import Link from "next/link";import {ResetPasswordForm} from "./reset-password-form";export const metadata={title:"Reset password | NEFE"};export default async function ResetPasswordPage({searchParams}:{searchParams:Promise<{token?:string}>}){const {token=""}=await searchParams;return <main className="auth-page"><section className="auth-card"><Link className="auth-brand" href="/">NEFE</Link><div><span>Account recovery</span><h1>Choose a new password</h1><p>Reset links are single-use and expire after 60 minutes.</p></div>{token?<ResetPasswordForm token={token}/>:<p className="auth-error">This reset link is incomplete.</p>}<p className="auth-secondary"><Link href="/forgot-password">Request another link</Link></p></section></main>}
