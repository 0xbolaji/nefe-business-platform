import Link from "next/link";import {notFound} from "next/navigation";import {getWorkspaceData} from "@/app/lib/data/workspace-repository";import {FavoriteButton,ViewTracker} from "../../_components/entity-actions";import {AppCard,DataTable,PageHeader,SectionHeader,StatBadge,StatusBadge} from "../../_components/ui";
export default async function BusinessProfilePage({params}:{params:Promise<{businessId:string}>}){const [{businessId},data]=await Promise.all([params,getWorkspaceData()]);const business=data.businesses.find(item=>item.id===businessId);if(!business)notFound();const industry=data.industries.find(item=>item.id===business.industryId);const currentCampaigns=data.campaigns.filter(item=>item.businessIds.includes(business.id));const currentJourneys=data.journeys.filter(item=>item.businessIds.includes(business.id));const currentOpportunities=data.opportunities.filter(item=>item.businessIds.includes(business.id));const relevantRecommendations=data.recommendations.filter(item=>item.recommendedBusinessId===business.id||item.opportunityId&&currentOpportunities.some(opp=>opp.id===item.opportunityId));return <><ViewTracker type="business" id={business.id} title={business.name} href={`/workspace/businesses/${business.id}`}/><PageHeader eyebrow="Business profile" title={business.name} description={business.description} action={<div className="ws-action-bar"><FavoriteButton type="business" id={business.id}/><Link className="ws-button" href={`/workspace/relationships/${business.id}`}>View relationships</Link><button className="ws-button primary">Connect</button></div>}/><div className="ws-profile-summary"><AppCard><div className="ws-business-hero"><span className="ws-avatar">{business.initials}</span><div><StatusBadge tone={business.status}>{business.status}</StatusBadge><h2>{business.name}</h2><p>{industry?.name} · {business.location}</p><div className="ws-tags">{business.tags.map(tag=><span key={tag}>{tag}</span>)}</div></div></div><div className="ws-detail-grid ws-profile-stats"><StatBadge label="Verification" value={business.verification}/><StatBadge label="Partner score" value={`${business.partnerScore}/100`}/><StatBadge label="Opportunity score" value={`${business.opportunityScore}/100`}/><StatBadge label="Connected partners" value={String(business.partnerCount)}/></div></AppCard><AppCard><SectionHeader title="Services and products" description="Persisted commercial capabilities."/><div className="ws-split-list"><div><h3>Services</h3>{business.services.map(item=><p key={item}>✓ {item}</p>)}</div><div><h3>Products</h3>{business.products.map(item=><p key={item}>◇ {item}</p>)}</div></div></AppCard></div><div className="ws-layout-2"><AppCard><SectionHeader title="Current opportunities" description={`${currentOpportunities.length} opportunities involve this business.`}/><DataTable label="Business opportunities" headers={["Opportunity","Stage","Value signal"]}>{currentOpportunities.map(item=><tr key={item.id}><td><Link href={`/workspace/opportunities/${item.id}`}><strong>{item.title}</strong></Link></td><td><StatusBadge tone={item.status}>{item.stage}</StatusBadge></td><td>AED {item.estimatedValue.toLocaleString()}</td></tr>)}</DataTable></AppCard><AppCard><SectionHeader title="Campaigns and journeys" description="Persisted collaboration participation."/><div className="ws-list">{currentCampaigns.map(item=><Link href={`/workspace/campaigns/${item.id}`} className="ws-list-item" key={item.id}><span>C</span><div><strong>{item.title}</strong><p>{item.objective}</p></div></Link>)}{currentJourneys.map(item=><Link href={`/workspace/journeys/${item.id}`} className="ws-list-item" key={item.id}><span>J</span><div><strong>{item.name}</strong><p>{item.coverage}% coverage</p></div></Link>)}</div></AppCard></div><div className="ws-layout-2"><AppCard><SectionHeader title="Recommendations" description="Persisted next actions for this profile."/><div className="ws-list">{relevantRecommendations.map(item=><div className="ws-list-item" key={item.id}><span>◇</span><div><strong>{item.title}</strong><p>{item.description}</p></div><StatusBadge tone={item.status}>{item.status}</StatusBadge></div>)}</div></AppCard><AppCard><SectionHeader title="Contacts" description="Governed business contacts."/><div className="ws-list">{business.contacts.map(contact=><div className="ws-list-item" key={contact.email}><span>{contact.name[0]}</span><div><strong>{contact.name}</strong><p>{contact.role}</p><small>{contact.email}</small></div></div>)}</div></AppCard></div></>}
