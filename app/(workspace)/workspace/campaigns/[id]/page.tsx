import Link from "next/link";import {notFound} from "next/navigation";
import {formatAed} from "../../../_lib/format";import {getWorkspaceData} from "@/app/lib/data/workspace-repository";
import {FavoriteButton,ViewTracker} from "../../_components/entity-actions";import {AppCard,PageHeader,ProgressBar,SectionHeader,StatBadge,StatusBadge} from "../../_components/ui";
export default async function CampaignDetailPage({params}:{params:Promise<{id:string}>}){const [{id},data]=await Promise.all([params,getWorkspaceData()]);const campaign=data.campaigns.find(item=>item.id===id);if(!campaign)notFound();return <><ViewTracker type="campaign" id={campaign.id} title={campaign.title} href={`/workspace/campaigns/${campaign.id}`}/><PageHeader eyebrow="Campaign details" title={campaign.title} description={campaign.objective} action={<div className="ws-action-bar"><FavoriteButton type="campaign" id={campaign.id}/><Link className="ws-button" href="/workspace/campaigns">All campaigns</Link></div>}/><div className="ws-metric-grid"><AppCard className="ws-metric"><p>Status</p><strong><StatusBadge tone={campaign.status}>{campaign.status}</StatusBadge></strong><small>{campaign.startDate} — {campaign.endDate}</small></AppCard><AppCard className="ws-metric"><p>Budget</p><strong>{formatAed(campaign.budget)}</strong></AppCard><AppCard className="ws-metric"><p>Modeled value</p><strong>{formatAed(campaign.modeledValue)}</strong></AppCard><AppCard className="ws-metric"><p>Progress</p><strong>{campaign.progress}%</strong><ProgressBar value={campaign.progress}/></AppCard></div><div className="ws-layout-2"><AppCard><SectionHeader title="Participants" description="Persisted businesses accountable for the campaign."/><div className="ws-list">{campaign.businessIds.map(businessId=>{const business=data.businesses.find(item=>item.id===businessId);return business?<Link className="ws-list-item" href={`/workspace/businesses/${business.id}`} key={business.id}><span>{business.initials}</span><div><strong>{business.name}</strong><p>{business.category} · {business.location}</p></div></Link>:null})}</div></AppCard><AppCard><SectionHeader title="Performance summary" description="Deterministic measures from persisted campaign data."/><div className="ws-detail-grid">{campaign.kpis.map(kpi=><StatBadge key={kpi.label} label={kpi.label} value={kpi.value}/>)}</div></AppCard></div></>}
