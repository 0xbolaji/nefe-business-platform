"use client";
import Link from "next/link";import {useEffect,useMemo,useRef,useState,type KeyboardEvent as ReactKeyboardEvent} from "react";import {useCollaboration} from "./collaboration-state";
const navigation=[{title:"Businesses",subtitle:"Commercial network",href:"/workspace/businesses",type:"Navigate"},{title:"Pilots",subtitle:"Pilot execution",href:"/workspace/pilots",type:"Navigate"},{title:"Create pilot",subtitle:"Convert an approved opportunity",href:"/workspace/pilots/new",type:"Navigate"},{title:"Campaigns",subtitle:"Commercial activity",href:"/workspace/campaigns",type:"Navigate"},{title:"Journeys",subtitle:"Customer paths",href:"/workspace/journeys",type:"Navigate"},{title:"Analytics",subtitle:"Performance reporting",href:"/workspace/analytics",type:"Navigate"},{title:"Executive insights",subtitle:"Commercial intelligence",href:"/workspace/executive-insights",type:"Navigate"},{title:"Compare opportunities",subtitle:"Decision support",href:"/workspace/opportunities/compare",type:"Navigate"},{title:"Settings",subtitle:"Workspace administration",href:"/workspace/settings",type:"Navigate"}];
export default function CommandPalette(){const [open,setOpen]=useState(false);const [query,setQuery]=useState("");const inputRef=useRef<HTMLInputElement>(null);const triggerRef=useRef<HTMLButtonElement>(null);const dialogRef=useRef<HTMLElement>(null);const {recentSearches,recordSearch,data}=useCollaboration();const searchItems=useMemo(()=>[...data.businesses.map(item=>({title:item.name,subtitle:`${data.industries.find(industry=>industry.id===item.industryId)?.name??item.category} · ${item.tags.join(" · ")}`,href:`/workspace/businesses/${item.id}`,type:"Business"})),...data.campaigns.map(item=>({title:item.title,subtitle:item.objective,href:`/workspace/campaigns/${item.id}`,type:"Campaign"})),...data.opportunities.map(item=>({title:item.title,subtitle:`${item.stage} · ${item.summary}`,href:`/workspace/opportunities/${item.id}`,type:"Opportunity"})),...data.journeys.map(item=>({title:item.name,subtitle:`${item.coverage}% coverage · ${item.touchpoints} touchpoints`,href:`/workspace/journeys/${item.id}`,type:"Journey"})),...data.recommendations.map(item=>({title:item.title,subtitle:item.description,href:"/workspace/recommendations",type:"Recommendation"})),...data.pilots.map(item=>({title:item.name,subtitle:`${item.status} · ${item.objective}`,href:`/workspace/pilots/${item.id}`,type:"Pilot"})),...data.executiveDecisions.map(item=>({title:item.title,subtitle:`${item.status.replaceAll("_"," ")} · ${item.requestedOutcome}`,href:`/workspace/decisions/${item.id}`,type:"Decision"})),...data.users.map(item=>({title:item.name,subtitle:`${item.role} · ${item.email}`,href:"/workspace/settings",type:"User"}))],[data]);const dismiss=()=>{setOpen(false);setQuery("");requestAnimationFrame(()=>triggerRef.current?.focus())};useEffect(()=>{const onKey=(event:KeyboardEvent)=>{if((event.metaKey||event.ctrlKey)&&event.key.toLowerCase()==="k"){event.preventDefault();setOpen(value=>!value)}if(event.key==="Escape"&&open)dismiss()};window.addEventListener("keydown",onKey);return()=>window.removeEventListener("keydown",onKey)},[open]);useEffect(()=>{if(open)requestAnimationFrame(()=>inputRef.current?.focus())},[open]);const results=useMemo(()=>{const source=query.trim()?searchItems:navigation;const term=query.toLowerCase();return source.filter(item=>`${item.title} ${item.subtitle} ${item.type}`.toLowerCase().includes(term)).slice(0,10)},[query,searchItems]);const close=(title:string)=>{recordSearch(query||title);dismiss()};const trapFocus=(event:ReactKeyboardEvent)=>{if(event.key!=="Tab"||!dialogRef.current)return;const focusable=[...dialogRef.current.querySelectorAll<HTMLElement>('a[href],button:not([disabled]),input:not([disabled])')];if(!focusable.length)return;const first=focusable[0],last=focusable.at(-1)!;if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}};return <><button ref={triggerRef} type="button" className="ws-search-trigger" onClick={()=>setOpen(true)} aria-haspopup="dialog" aria-expanded={open}><span aria-hidden="true">⌕</span><b>Search workspace</b><kbd>⌘ K</kbd></button>{open&&<div className="ws-command-backdrop" onMouseDown={event=>{if(event.target===event.currentTarget)dismiss()}}><section ref={dialogRef} role="dialog" aria-modal="true" aria-label="Workspace search" className="ws-command" onKeyDown={trapFocus}><label><span aria-hidden="true">⌕</span><input ref={inputRef} value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search businesses, opportunities, campaigns, journeys…" aria-label="Search workspace"/><button type="button" className="ws-command-close" onClick={dismiss} aria-label="Close workspace search">Esc</button></label>{!query&&recentSearches.length>0&&<div className="ws-command-recent"><small>Recent searches</small>{recentSearches.map(item=><button type="button" key={item} onClick={()=>setQuery(item)}>{item}</button>)}</div>}<div className="ws-command-results" role="list">{results.map(item=><Link href={item.href} key={`${item.type}-${item.title}`} onClick={()=>close(item.title)}><span aria-hidden="true">{item.type.slice(0,2)}</span><div><strong>{item.title}</strong><small>{item.subtitle}</small></div><i aria-hidden="true">↵</i></Link>)}{results.length===0&&<div className="ws-empty" role="status"><span aria-hidden="true">⌕</span><h3>No workspace results</h3><p>Try a business name, industry, campaign, journey or opportunity.</p></div>}</div><footer aria-hidden="true"><span>Tab Navigate</span><span>Enter Open</span><span>Esc Close</span></footer></section></div>}</>}
