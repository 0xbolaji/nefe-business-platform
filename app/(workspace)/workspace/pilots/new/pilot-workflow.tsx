"use client";import {useRouter,useSearchParams} from "next/navigation";import {useState} from "react";import {analyzeEcosystem,scoreOpportunity} from "../../../_lib/commercial-intelligence";import {formatAed} from "../../../_lib/format";import type {Pilot,PilotParticipant} from "../../../_lib/models";import {useCollaboration} from "../../_components/collaboration-state";import {usePilots} from "../../_components/pilot-state";import {AppCard,PageHeader,SectionHeader,StatusBadge} from "../../_components/ui";
const steps=["Select opportunity","Confirm participants","Define objectives","Timeline and milestones","Configure KPIs","Assign owners","Risks and dependencies","Review and create"];
export default function NewPilotWorkflow(){const {data}=useCollaboration();const eligible=data.opportunities.filter(item=>item.stage==="Approved"||item.stage==="Pilot");const params=useSearchParams();const router=useRouter();const {createPilot}=usePilots();const initial=eligible.find(item=>item.id===params.get("opportunity"))??eligible[0];const [step,setStep]=useState(0);const [opportunityId,setOpportunityId]=useState(initial?.id??"");const opportunity=data.opportunities.find(item=>item.id===opportunityId);const [participantIds,setParticipantIds]=useState<string[]>(initial?.businessIds??[]);const [objective,setObjective]=useState(initial?`Validate ${initial.title.toLowerCase()} as a controlled real-world commercial pilot.`:"");const [startDate,setStartDate]=useState("2026-09-01");const [endDate,setEndDate]=useState("2026-11-30");const [ownerId,setOwnerId]=useState(initial?.ownerId??data.users[0]?.id??"");if(!opportunity)return <><PageHeader title="Create pilot" description="No approved opportunity is currently available."/><AppCard><p>Approve an opportunity before creating a pilot.</p></AppCard></>;const intelligence=scoreOpportunity(opportunity,data);const ecosystem=analyzeEcosystem(participantIds,data);const participants:PilotParticipant[]=participantIds.map(id=>({businessId:id,role:"Experience Partner",status:"Pending"}));const create=()=>{const id=crypto.randomUUID();const pilot:Pilot={id,name:`${opportunity.title} Pilot`,opportunityId:opportunity.id,regionId:data.regions[0]?.id??"",ownerId,status:"Draft",objective,commercialRationale:opportunity.commercialReasoning,journeyImpact:opportunity.journeyImpact,participantIds,participants,modeledValue:opportunity.estimatedValue,startDate,endDate,reviewDate:endDate,milestones:[],tasks:[],kpis:[],approvals:[],risks:[],updates:[],decisions:[]};createPilot(pilot);router.push(`/workspace/pilots/${id}`)};return <><PageHeader eyebrow="Create pilot" title="Convert an approved opportunity into an execution plan" description="The completed plan is persisted to the authenticated workspace."/><nav className="ws-stepper" aria-label="Pilot creation steps">{steps.map((label,index)=><button key={label} className={index===step?"active":index<step?"complete":""} onClick={()=>index<=step&&setStep(index)}><span>{index+1}</span>{label}</button>)}</nav><AppCard>{step===0&&<><SectionHeader title="Select opportunity" description="Only approved or pilot-stage opportunities are eligible."/><div className="ws-selection-grid">{eligible.map(item=><button className={opportunityId===item.id?"active":""} key={item.id} onClick={()=>{setOpportunityId(item.id);setParticipantIds(item.businessIds)}}><StatusBadge tone={item.status}>{item.stage}</StatusBadge><strong>{item.title}</strong><small>{formatAed(item.estimatedValue)} · {scoreOpportunity(item,data).confidence}% confidence</small></button>)}</div></>}{step===1&&<><SectionHeader title="Confirm participants" description="Persisted opportunity participants are preselected."/><div className="ws-what-if-businesses">{data.businesses.map(item=><button key={item.id} aria-pressed={participantIds.includes(item.id)} className={participantIds.includes(item.id)?"active":""} onClick={()=>setParticipantIds(current=>current.includes(item.id)?current.filter(id=>id!==item.id):[...current,item.id])}><span className="ws-avatar">{item.initials}</span><b>{item.name}</b></button>)}</div></>}{step===2&&<><SectionHeader title="Define objectives" description="Commercial rationale carries forward from the opportunity."/><label className="ws-field"><span>Pilot objective</span><textarea value={objective} onChange={e=>setObjective(e.target.value)}/></label></>}{step===3&&<><SectionHeader title="Set timeline and milestones" description="Define the controlled execution period."/><div className="ws-form-grid"><label className="ws-field"><span>Start</span><input type="date" value={startDate} onChange={e=>setStartDate(e.target.value)}/></label><label className="ws-field"><span>End</span><input type="date" value={endDate} onChange={e=>setEndDate(e.target.value)}/></label></div></>}{step===4&&<><SectionHeader title="Configure KPIs" description="Deterministic planning measures remain independent of persistence."/><p>Modeled value: {formatAed(opportunity.estimatedValue)} · Journey coverage: {ecosystem.scores.journeyCoverage}%</p></>}{step===5&&<><SectionHeader title="Assign owners" description="One accountable pilot owner is required."/><select value={ownerId} onChange={e=>setOwnerId(e.target.value)}>{data.users.map(item=><option value={item.id} key={item.id}>{item.name}</option>)}</select></>}{step===6&&<><SectionHeader title="Review risks and dependencies" description="Known limiting factors from deterministic opportunity analysis."/><div className="ws-list">{intelligence.limitingFactors.map(item=><p key={item}>{item}</p>)}</div></>}{step===7&&<><SectionHeader title="Review and create pilot" description="Confirm the persisted plan before creation."/><p><b>{opportunity.title}</b></p><p>{participantIds.length} participants · {startDate} to {endDate}</p></>}<div className="ws-workflow-actions"><button className="ws-button" disabled={step===0} onClick={()=>setStep(v=>v-1)}>Back</button>{step<7?<button className="ws-button primary" onClick={()=>setStep(v=>v+1)}>Continue</button>:<button className="ws-button primary" onClick={create}>Create pilot</button>}</div></AppCard></>}
