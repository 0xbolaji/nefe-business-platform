"use client";import {useActionState} from "react";import {useFormStatus} from "react-dom";import {verifyEmail,type VerifyState} from "./actions";const initial:VerifyState={error:""};function Submit(){const {pending}=useFormStatus();return <button disabled={pending}>{pending?"Verifying…":"Verify email"}</button>}export function VerifyEmailForm({token}:{token:string}){const [state,action]=useActionState(verifyEmail,initial);return <form action={action}><input type="hidden" name="token" value={token}/>{state.error&&<p className="auth-error" role="alert">{state.error}</p>}<Submit/></form>}
