import Link from "next/link";import {VerifyEmailForm} from "./verify-email-form";export const metadata={title:"Verify email | NEFE"};export default async function VerifyEmailPage({searchParams}:{searchParams:Promise<{token?:string}>}){const {token=""}=await searchParams;return <main className="auth-page"><section className="auth-card"><Link className="auth-brand" href="/">NEFE</Link><div><span>Account verification</span><h1>Verify your email</h1><p>Confirm your email address to activate your organization membership.</p></div>{token?<VerifyEmailForm token={token}/>:<p className="auth-error">This verification link is incomplete.</p>}<p className="auth-secondary"><Link href="/sign-in">Back to sign in</Link></p></section></main>}
