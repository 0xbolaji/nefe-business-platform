"use client";import Link from "next/link";import {useState} from "react";import {useFormStatus} from "react-dom";import {authenticate} from "./actions";function Submit(){const {pending}=useFormStatus();return <button disabled={pending} aria-disabled={pending}>{pending?"Signing in…":"Sign in"}</button>}export function SignInForm({returnTo}:{returnTo:string}){const [visible,setVisible]=useState(false);return <form action={authenticate}><input type="hidden" name="returnTo" value={returnTo}/><label><span>Email <b className="auth-required" aria-hidden="true">*</b></span><input name="email" type="email" autoComplete="username" required/></label><label><span>Password <b className="auth-required" aria-hidden="true">*</b></span><span className="auth-password"><input name="password" type={visible?"text":"password"} autoComplete="current-password" minLength={12} required/><button type="button" onClick={()=>setVisible(v=>!v)}>{visible?"Hide":"Show"}</button></span></label><div className="auth-form-row"><label className="auth-check"><input name="rememberMe" type="checkbox"/><span>Remember me for 30 days</span></label><Link href="/forgot-password">Forgot password?</Link></div><Submit/></form>}
